<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.bx</groupId>
    <artifactId>ultimatevirtualspawner</artifactId>
    <version>1.0</version>
    <packaging>jar</packaging>

    <name>ultimatevirtualspawner</name>
    <description>Virtual mob spawners with stacking, virtual storage, filters and selling.</description>

    <properties>
        <java.version>21</java.version>
        <spigot-api.version>1.21.10-R0.1-SNAPSHOT</spigot-api.version>
        <folia-api.version>1.21.11-R0.1-SNAPSHOT</folia-api.version>
        <placeholderapi.version>2.12.3</placeholderapi.version>
        <server-api.groupId>org.spigotmc</server-api.groupId>
        <server-api.artifactId>spigot-api</server-api.artifactId>
        <server-api.version>${spigot-api.version}</server-api.version>
        <source.directory>src/main/java</source.directory>
        <folia.supported>true</folia.supported>
        <minecraft.api.version>1.21.10</minecraft.api.version>
        <server.variant>paper-spigot</server.variant>
        <server.supported.range>1.21.10-26.2</server.supported.range>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <paper.supported.min>1.21.10</paper.supported.min>
        <paper.supported.max>26.2</paper.supported.max>
        <folia.supported.min>1.21.11</folia.supported.min>
        <folia.supported.max>26.2</folia.supported.max>
        <folia.supported.range>1.21.11-26.2</folia.supported.range>

        <sqlite.version>3.53.2.1</sqlite.version>
        <mysql.version>9.5.0</mysql.version>
    </properties>

    <build>
        <defaultGoal>clean package</defaultGoal>
        <finalName>UltimateVirtualSpawner-${project.version}</finalName>
        <sourceDirectory>${source.directory}</sourceDirectory>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.6</version>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>plugin.yml</include>
                    <include>compatibility.properties</include>
                </includes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>plugin.yml</exclude>
                    <exclude>compatibility.properties</exclude>
                </excludes>
            </resource>
        </resources>
    </build>

    <repositories>
        <repository>
            <id>spigot-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>codemc</id>
            <url>https://repo.codemc.org/repository/maven-public/</url>
        </repository>
        <repository>
            <id>placeholderapi</id>
            <url>https://repo.extendedclip.com/content/repositories/placeholderapi/</url>
        </repository>
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>${server-api.groupId}</groupId>
            <artifactId>${server-api.artifactId}</artifactId>
            <version>${server-api.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>me.clip</groupId>
            <artifactId>placeholderapi</artifactId>
            <version>${placeholderapi.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.github.MilkBowl</groupId>
            <artifactId>VaultAPI</artifactId>
            <version>1.7</version>
            <scope>provided</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.bukkit</groupId>
                    <artifactId>bukkit</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.11.4</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
